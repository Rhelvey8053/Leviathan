/goal The pre-Claude flag logic is made configurable with multiple selection modes, the 1b test suite is extended to pin every mode, and all modes are run against the saved snapshot with candidate counts reported side by side in reports/flag_modes.md (printed in full in the transcript). Work on a branch named flag-modes-1d with small descriptive commits.

READ-ONLY everywhere: no order/trade endpoints, no writes to the real leviathan.db. The sweep/comparison step must run fully offline against the existing saved snapshot in data/snapshots/ — never hit the network.

STEP 1 — CONFIG-DRIVEN FLAG MODES: refactor scanner.score_market so its flag condition is driven by a new config key markets.flag_mode with these modes, and NO mode is hardcoded as the only behavior:
  - "passthrough" — current behavior: flag if raw_edge>threshold OR base_rate is None OR drift. (Preserve exactly, as the baseline to compare against — do not delete it.)
  - "strict_anomaly_only" — flag ONLY if a real anomaly fired: raw_edge>edge_threshold, OR drift_flag, OR whale signal present. A base_rate-None market with no anomaly does NOT flag. base_rate remains available as context passed to Claude but is not itself a trigger.
  - "strict_with_heuristic" — same as strict_anomaly_only, PLUS: if estimate_base_rate returns a value and abs(base_rate - mid) > edge_threshold, that also flags.
Default the config to "passthrough" so nothing changes silently until Reed chooses. Whale state may not be present at score time — document clearly how each mode treats whale info given the pipeline order in main.py, and if whale isn't available in score_market, note it and have strict modes rely on edge/drift (+ heuristic) accordingly rather than silently always-false on whale.

STEP 2 — TESTS: extend the pytest suite (offline, tmp DB, no network) to pin each mode independently. For a constructed set of markets — one pure BR_NONE no-anomaly market, one with raw_edge over threshold, one with drift, one with a base_rate-implied edge, one totally inert — assert exactly which modes flag it and which do not. The BR_NONE-no-anomaly market must flag under passthrough and must NOT flag under strict_anomaly_only. pytest must exit 0 with output shown.

STEP 3 — COMPARE ON SNAPSHOT: write/extend an analysis script that loads the most recent saved snapshot and runs filter_markets + score_markets under ALL THREE modes, producing reports/flag_modes.md with: a table showing, per mode, total markets surviving filter, total flagged, and the flag-path breakdown (edge / drift / whale / heuristic / none); and a short plain-language read of what each mode would forward to Claude. Include a VERDICT section stating, for each mode, the candidate count at production thresholds, and noting explicitly that this measures candidate VOLUME and selectivity only — correctness still requires resolved outcomes. Do NOT call the claude CLI or any scorer anywhere in this goal.

Append to PROGRESS.md: the candidate counts per mode, a recommendation, and the top 3 next steps. Do not change the default flag_mode away from passthrough — leave that choice to Reed. Or stop after 22 turns.