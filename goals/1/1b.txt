/goal A pytest suite exists and passes for the logger and scanner modules, proven by pytest exiting 0 with output shown in the transcript. Work on a branch named tests-1b; commit with small descriptive messages; do NOT modify any production source file except to add a docstring or fix an outright bug you can prove with a failing-then-passing test (and if you do fix such a bug, call it out explicitly in the transcript and in PROGRESS.md — do not change behavior silently). Constraints: tests must run fully offline — never call Kalshi, Polymarket, Manifold, PredictIt, OddsAPI, Metaculus, the claude CLI, or send email; mock or stub every network and subprocess boundary. Use a temporary throwaway SQLite file or in-memory DB for logger tests, never the real leviathan.db. Required coverage: (1) resolve_outcomes payoff math — assert all four cases to the cent: YES bought at 0.30 resolving YES = +0.70, YES at 0.30 resolving NO = -0.30, NO bought at 0.30 (cost 0.70) resolving NO = +0.30, NO at 0.30 resolving YES = -0.70; plus a PASS/blank direction yields 0.0; verify it reads market_price not edge. (2) resolve_outcomes only touches rows where outcome is null or empty and leaves already-resolved rows unchanged. (3) get_stats — win_rate is null with zero resolved, computes correctly with a mix of WIN/LOSS rows, and resolved counts match how log_signal writes blanks. (4) scanner.filter_markets — markets outside the [min_market_price, max_market_price] bounds are dropped and ones inside are kept; volume floors per bucket are applied; close-time bounds are honored; a market with no close_time is dropped. (5) scanner.classify_time_horizon and score_market flag logic — confirm what actually triggers flag=True now that the price-bounds gate exists. (6) a regression test pinning the per-$1 payoff convention so if anyone later changes the notional or the report multiplier, a test documents the expected unit. Add a short tests/README noting how to run them (python -m pytest -q) and that they are offline-only. Also append to PROGRESS.md: what is now covered, any bug found, and the top 3 recommended next steps. Or stop after 20 turns.