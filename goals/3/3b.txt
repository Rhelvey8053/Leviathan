/goal The winning-wallet selection criterion is fixed so it ranks on RESOLVED outcomes instead of unrealized P&L on open positions, and excludes coin-flip markets — proven by a printed before/after comparison of which wallets qualify under the old vs new criterion, and a passing offline test suite (pytest exit 0, output shown). Work on a branch named smart-money-fix-3b with small descriptive commits. READ-ONLY on all external platforms: Polymarket/account data via existing read endpoints only, never any order/trade endpoint. The only data mutation permitted is rewriting the winning-wallet cache (winning_accounts.json) — back it up to winning_accounts.json.bak_3b first. Run this AFTER resolve-first-3a has completed and committed; this is a separate branch and must not be run concurrently on the same working tree.

SCOPE — this fixes ONE known bug, adds nothing new. This goal is NOT permission to add heuristics, calibration rules, scoring dimensions, new signals, or new analytics. It repairs the wallet-selection defect documented in AUDIT.md Section 5. The freeze from 3a still applies to everything except the specific accounts.py selection logic named below. If you find yourself adding a new signal type or a new market-scoring feature, STOP — out of scope.

CONTEXT — the bug (AUDIT.md Section 5, do not re-litigate). _score_wallet (accounts.py:71) computes avg_pct_pnl and total_cash_pnl across ALL positions (open + closed), and win_rate only from resolved positions. _is_winner (accounts.py:130) requires position_count>=3, avg_pct_pnl>=10.0, total_cash_pnl>=25.0 — none of which require RESOLVED positions. The live cache confirms the failure: all 3 cached "winners" have resolved_count: 0 and win_rate: null. The ranking is on unrealized P&L of open positions, so a wallet holding a 5-minute Bitcoin Up/Down bet that happened to 10x is indistinguishable from a skilled forecaster. The June 17/18 smart-money files show the same disease live: the tracked wallets' books are dominated by World Cup/tennis match bets and already-settled Iran-peace positions priced at 1.00. The system is selecting for luck and survivorship, not skill.

EXPECTED HONEST OUTCOME — read this before you start. Applying a resolved-outcome filter to wallets that were selected on the WRONG criterion may legitimately produce an EMPTY qualifying set. That is the correct answer, not a failure. If zero currently-cached wallets have resolved_count >= the threshold with a positive resolved win rate, report "NO VERIFIED SMART MONEY YET" plainly and write an empty (or near-empty) watchlist. DO NOT loosen the threshold until something passes — loosening-until-something-qualifies is exactly how the luck-selection bug gets reintroduced. An empty watchlist honestly says "we don't yet have wallets with a verified track record to tail," which is a true and useful statement.

PART A — INSTRUMENT THE CURRENT STATE (no changes yet). Read winning_accounts.json and, for every cached wallet, print: address, position_count, resolved_count, win_rate, avg_pct_pnl, total_cash_pnl. Then state plainly how many would survive a resolved-outcome filter (resolved_count >= 10) — expected answer based on the audit: zero or near-zero. This is the "before" half of the before/after.

PART B — FIX THE SELECTION CRITERION (accounts.py only). Change _is_winner / the ranking so that:
  1. A wallet must have resolved_count >= a configurable minimum (config key accounts.min_resolved_positions, default 10) to qualify at all. Wallets with fewer resolved positions are EXCLUDED regardless of unrealized P&L.
  2. Ranking is on RESOLVED win rate (and/or realized cash P&L on resolved positions), NOT avg_pct_pnl across open positions. Make the ranking metric explicit in a comment.
  3. Coin-flip / luck-dominated markets are excluded from the scoring entirely: positions whose market titles match sub-daily crypto tick patterns ("Up or Down", "5m", "1m", "hourly") OR sports-game patterns (reuse the existing _SPORTS_TITLE_PATTERNS / _is_sports_title logic from smart_money_scan.py rather than writing a new list) do not count toward a wallet's track record. A wallet whose entire resolved history is coin-flips has no verified forecasting skill.
  4. Raise accounts.min_cash_pnl off the trivial $25 floor to a configurable value (default your choice, justified in one comment line) — $25 of realized P&L is achievable on pure luck.
Do not invent new wallet-quality metrics beyond resolved win rate, resolved cash P&L, and the exclusion filters above. The goal is to fix WHAT is measured, not to add new measurements.

PART C — REGENERATE THE CACHE. Re-run the existing discovery/scoring path (read-only) under the new criterion and rewrite winning_accounts.json. Print the "after" half of the comparison: which wallets now qualify, their resolved_count and resolved win rate, and how many were dropped versus the old cache. If the qualifying set is empty, write an empty watchlist and print "NO VERIFIED SMART MONEY YET — zero wallets meet resolved_count >= N with positive resolved win rate." Then state the one-line implication: the smart-money cross-reference layer should be treated as OFF until verified wallets exist, so main.py's watchlist boost currently has no trustworthy input.

PART D — TESTS (offline, tmp data, no network — mock any fetch). Assert:
  - a wallet with resolved_count below the threshold is excluded even if its open-position avg_pct_pnl is very high (the core regression — this is the exact luck case the old code passed);
  - a wallet with resolved_count above the threshold and a real resolved win rate qualifies;
  - a coin-flip-only resolved history (crypto tick / sports titles) does NOT count toward qualification;
  - ranking orders wallets by resolved win rate, not by open-position P&L (construct two wallets where those two orderings disagree and assert the resolved ordering wins);
  - an empty qualifying set produces an empty watchlist without error (the honest-empty case must not crash downstream).
pytest exits 0 with output shown. Existing tests must still pass untouched — if any existing test encoded the old luck-based behavior, do NOT silently rewrite it; call it out in the transcript and in PROGRESS.md as a behavior change with justification.

PART E — REPORT. Append to docs/PROGRESS.md: the before/after wallet comparison (how many qualified under old vs new criterion), whether the result was a populated or empty watchlist, the explicit statement that NO new signal/heuristic/scoring code was added (only the accounts.py selection criterion and its config keys), and the implication for the watchlist boost in main.py. Top 3 next steps should be about ACCUMULATING verified wallets (e.g. "re-run discovery as more positions resolve," "do not re-enable watchlist boost until N verified wallets exist") — not about new signals.

Or stop after 20 turns.