/goal export_to_csv.py is updated with a column whitelist so only 
analysis-relevant columns are written to signals.csv — internal 
pipeline plumbing columns are dropped before export. Proven by: 
re-running the script produces a signals.csv with only whitelisted 
columns, and pytest exits 0.

SCOPE — export_to_csv.py only. No changes to any other file, 
DB schema, or pipeline logic. Columns are dropped from the CSV 
output only — the DB is never touched.

PART A — apply whitelist

After all computed columns are added (is_resolved, is_win, 
confidence_rank, horizon_rank, date, pnl_scaled, lv_band) and 
before writing to CSV, filter the dataframe to only these columns 
(in this order, skip any that don't exist in the dataframe):

    call_id, date, timestamp, ticker, title,
    source, direction, confidence, confidence_rank,
    flag_path, time_horizon, horizon_rank,
    market_price, edge, net_edge, base_rate,
    result, is_resolved, is_win, pnl_if_traded, pnl_scaled,
    leviathan_score, lv_band,
    close_time, sig_edge, sig_drift, sig_br_none,
    watchlist_signal, whale_detected,
    heuristic_label, short_horizon

Use: signals = signals[[c for c in WHITELIST if c in signals.columns]]

Define WHITELIST as a module-level constant at the top of the file 
so it is easy to add or remove columns later without touching the 
function body.

PART B — TESTS (offline, tmp DB, no network)

Assert:
  - Output CSV contains only whitelisted columns
  - None of the dropped columns appear 
    (run_id, from_signal, fill_count, fill_fee, contract_type,
    segment, outcome, our_estimate, direction_aligned, entry_price,
    signal_call_id, logged_under, resolution_date, whale_direction,
    heuristic_direction)
  - All computed columns are present (is_win, is_resolved, 
    lv_band, pnl_scaled, confidence_rank, horizon_rank, date)
  - Running twice does not error

pytest exits 0 with output shown.

Or stop after 8 turns.