/goal The flag-path attribution bug is fixed and the drift signal is recalibrated to be selective, proven by an updated reports/flag_modes.md (printed in full) showing the true per-signal fire rates and a drift threshold sweep. Work on a branch named drift-fix-1e with small descriptive commits. READ-ONLY everywhere: no order/trade endpoints, no writes to the real leviathan.db; all measurement runs offline against the existing saved snapshot in data/snapshots/ — never hit the network. Do NOT call the claude CLI or any scorer anywhere in this goal.

PART A — FIX ATTRIBUTION FIRST: the current flag_path records only the first branch that fires, so the same snapshot shows DRIFT=0 under passthrough but DRIFT=18 under strict — the counts measure branch order, not which signals actually fired. Change score_market so each market records ALL signals present (e.g. a list/flags: edge, drift, heuristic, and whale-if-available), independent of which mode is active and independent of branch order. The mode still decides flag=True/False, but the per-signal presence must be reported truthfully and identically regardless of mode. Add/extend offline pytest cases asserting that for a constructed market with BOTH an edge and a drift present, both are reported under every mode — pinning that attribution no longer depends on ordering. pytest exits 0 with output shown.

PART B — DIAGNOSE DRIFT: using the saved snapshot, show the distribution of drift magnitude across filtered markets and how it correlates with price level. Demonstrate the suspected root cause explicitly: that compute_drift_signal's (mid - last)/last test produces large percentage drift from tiny absolute moves at low prices, which is why ~86% of filtered markets flag. Put the evidence (e.g. drift-fire rate bucketed by price range) in the report.

PART C — RECALIBRATE DRIFT: change compute_drift_signal to require BOTH a minimum absolute move AND a minimum percentage move, with both values read from config (e.g. markets.drift_min_abs and markets.drift_min_pct), no hardcoded magic numbers. Then sweep a grid of at least 5 absolute thresholds x 5 percentage thresholds against the snapshot and produce a table of the resulting drift-fire rate (as % of filtered markets) for each combination. Do NOT unilaterally pick the final values — present the grid so Reed chooses. Include a recommended starting point with one-line reasoning, but leave the committed config at values that clearly mark themselves as provisional/unchosen.

PART D — REPORT: update reports/flag_modes.md with (1) the corrected attribution table showing true per-signal fire rates under each mode, (2) the drift-by-price-bucket diagnosis, (3) the threshold sweep grid, and a VERDICT stating the drift-fire rate at the recommended setting and whether any flag_mode is now actually selective. Reaffirm that this measures selectivity/volume only — correctness still needs resolved outcomes. Append to PROGRESS.md: the attribution bug fix, the chosen-pending drift calibration options, and the top 3 next steps. Do NOT change the default flag_mode — that decision stays with Reed and is blocked until drift is trustworthy. Or stop after 24 turns.