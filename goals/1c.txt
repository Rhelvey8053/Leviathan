/goal A threshold sweep over a saved Kalshi market snapshot is complete, with reports/threshold_sweep.md written and its summary table plus written verdict printed in full in the transcript. Work on a branch named sweep-1c with small descriptive commits.

READ-ONLY on all platforms: fetch Kalshi market data only, never call any order/trade endpoint, never write to the real leviathan.db. The only new persistent artifacts are the snapshot file, the report, and analysis scripts.

STEP 1 — SNAPSHOT: write analysis/snapshot_markets.py that pulls the current open-market universe from Kalshi the same way main.py does (events catalog, skip KXMVE event tickers, fetch event markets), and saves the COMPLETE raw market objects — not a trimmed subset — to data/snapshots/markets_YYYYMMDD_HHMMSS.json with a small header recording fetch timestamp, environment, event count, and market count. Capture full market dicts including price fields, volume fields, close_time, last_price, and previous_price so the file is reusable for the BG4 backtest. Print the snapshot path and market count. Make the script re-runnable (a new timestamped file each run, never overwriting).

STEP 2 — SWEEP: write analysis/threshold_sweep.py that loads the most recent snapshot from disk (never hits the network) and runs scanner.filter_markets + scanner.score_markets against a grid of at least 25 combinations spanning edge_threshold, min_market_price/max_market_price bounds, and the per-bucket volume floors. For each combination record: markets surviving filter, markets flagged (flag=True), and the breakdown of WHICH flag path fired (raw_edge over threshold vs base_rate-None-fallback vs drift). DO NOT call the claude CLI or any scorer — measure only the filter-and-flag funnel; stub scoring entirely.

STEP 3 — REPORT: write reports/threshold_sweep.md containing the grid as a table (combination -> survived, flagged, flag-path breakdown), and a written VERDICT section answering plainly: at the current production thresholds, how many markets flag, and is the pipeline now surfacing a workable candidate count or is it still dominated by the base_rate-None fallback firing on nearly everything? Note explicitly that this measures candidate VOLUME only — signal correctness cannot be judged until markets resolve. Recommend a specific threshold configuration with one-line reasoning, and flag if the flag logic itself (not the thresholds) is the thing that needs changing.

Tests/offline note: the sweep step must run with no network. Append to PROGRESS.md what the sweep found, the recommended config, and the top 3 next steps. Or stop after 22 turns.