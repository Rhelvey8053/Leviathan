/goal The daily report (core/report.py's compile_report and its helpers) is functionally correct but unreadable and, in the betting queue's case, incomplete: signal headers stack up to 7-8 independent bracketed labels with no visual hierarchy, the close-time/urgency calculation is copy-pasted near-verbatim in _signal_block AND _top_picks (a fix in one won't propagate to the other), every section builds its own ad-hoc column padding instead of one shared table renderer, a real rendered signal header line measured at 183 characters wide, at least three tables show a bare ticker with NO market title anywhere nearby (BETTING QUEUE, SMART MONEY DRIFT, Per-Trader Cross-References), and — the most serious finding — the BETTING QUEUE already fetches `direction` and `confidence` from the database but then DISCARDS both before rendering, so the single most decision-critical table in the report shows a price and an edge percentage with no indication of which side (YES/NO) is being recommended or how confident the model was. Looked at as a whole from an outside reader's perspective: every section representing an actual model SELECTION (NEW SIGNALS, REPEAT SIGNALS, TOP PICKS, BETTING QUEUE) must show, at minimum, BUY YES/NO, confidence, EV/edge, and — where it exists — a short analysis with sources; sections that are surveillance/context rather than selections (SHORT-TERM WATCHLIST, WHALE ACTIVITY) are correctly exempt and already say so in their headers. This goal does NOT change what data is shown or how anything is calculated, with one narrow exception: surfacing fields that are already fetched/stored but currently dropped before display is a presentation fix, not a new computation. Proven by: pytest exits 0, a printed before/after of the same synthetic signal block showing the line-length and label-stacking problem fixed, a full dry-run of compile_report against representative synthetic data (not requiring leviathan.db) that a human can read top to bottom without lines wrapping past 100 columns, and confirmation that every SELECTION-type row in every section — including the betting queue — shows direction, confidence, EV, and a title, not just a subset of those.

SCOPE — touch core/report.py, core/logger.py (ONLY to add a title/market_price lookup to the betting queue's existing SQL query — do not change any other query, table schema, or stored value), and test files. Do NOT change any value, threshold, calculation, or data source that feeds into the report's numbers (no changes to scanner.py, scorer.py, config.json, or what gets computed). This is a presentation-layer goal — the exceptions are pulling an existing `title` column into a query that currently omits it, and surfacing `direction`/`confidence` values the betting queue already fetches but discards — both are "show what's already there" fixes, not new calculations.

PART A — DIAGNOSE FIRST, IN THE TRANSCRIPT (no code yet). Before touching anything, do three things:
  1. Render the actual output of _signal_block against a synthetic signal dict that triggers as many optional flags as realistically co-occur (second_pass, confidence_downgraded, flag_path, short_horizon, signal_strength>=2, repeat_count>=2, drift+heuristic conflict, claude override, watchlist_signal, smart_money) — i.e. a worst-case-realistic signal, not a contrived one with impossible flag combinations. Print it raw, measure and state the longest line length, and identify by line number every place where _signal_block and _top_picks duplicate logic (close_time parsing, urgency labels, repeat label, flag_path label, signal_strength star label).
  2. Grep core/report.py for every place a ticker is printed and check, line by line, whether a title/description is shown anywhere on that same row or within a few lines above/below it. State explicitly, by line number, which sections already show title (expect: NEW SIGNALS via _signal_block, REPEAT SIGNALS, SHORT-TERM WATCHLIST, WHALE ACTIVITY, smart money's Kalshi Targets block) and which do NOT (expect: the BETTING QUEUE table in _betting_queue — note its SQL SELECT does not even fetch title from the DB — the SMART MONEY DRIFT table, and the Per-Trader Cross-References table, which has access to kalshi_title in its data but doesn't print it). Confirm or correct this list against what you actually find; do not assume the audit above is exhaustive.
  3. In _betting_queue specifically, trace `direction` and `confidence` from the SQL SELECT through to the rendered table. Confirm (or correct) that both are fetched from the DB, unpacked into local variables, used internally for EV/Kelly math in the case of direction, and then never added to the `candidates` dict and never rendered in the table header or rows. State this plainly — this is the single most consequential finding in this goal, since the betting queue is the section most directly used to decide what to act on.
Findings 1-3 must be stated plainly before PART B starts.

PART B — ONE SHARED LAYOUT TOOLKIT (the actual fix). Add a small set of internal helpers near the top of core/report.py (do not create a new module unless it's clearly cleaner — your call, but keep it in one place):
  1. A single _format_label_stack(labels: list[str], max_width: int = 90) -> list[str]
     that takes the various optional bracket labels as a list of (priority, text)
     or similar, and returns 1-2 lines instead of one unbounded line — group by
     priority so warnings/conflicts (CLAUDE OVERRIDE, SIGNAL CONFLICT,
     SPREAD > EDGE) are visually distinct from informational tags (★ strength,
     LV band, REPEAT count), and informational tags wrap onto a second line
     rather than running the header past ~90-100 chars. Decide and document
     in a comment what counts as "warning-tier" vs "info-tier" — this is a
     judgment call, make it explicitly rather than implicitly.
  2. A single _close_and_urgency(s: dict) -> tuple[str, str] (or similar) that
     both _signal_block and _top_picks call instead of each having their own
     copy of the close_time parsing / urgency threshold logic. Behavior must
     be IDENTICAL to current behavior (same thresholds: <=0 days, <=3, <=7) —
     this is a dedup, not a behavior change.
  3. A single _render_table(headers: list[str], rows: list[list[str]],
     widths: list[int] | None = None) -> list[str] that the SHORT-TERM
     WATCHLIST and WHALE ACTIVITY sections (and the betting queue, if it can
     be cleanly adapted without changing its filter logic from goal 4b) call
     instead of each hand-rolling f"{x:<28} {y:<9}" column padding. Auto-size
     columns to content with a sane max width per column rather than fixed
     magic-number widths, OR keep fixed widths if auto-sizing introduces
     misalignment risk — your call, but be consistent across all three
     sections using it.

PART C — APPLY THE TOOLKIT, SECTION BY SECTION. Rewrite _signal_block, _top_picks,
the SHORT-TERM WATCHLIST block, and the WHALE ACTIVITY block in compile_report
to use the PART B helpers. Specific readability fixes required:
  - Signal header: confidence/direction/horizon stays on its own clean line;
    warning-tier labels (conflicts, overrides, spread>edge) get their own
    visually distinct line directly under the header (not buried mid-stack);
    info-tier labels (★ strength, LV band, repeat count, flag_path) group
    together on a separate line, wrapped if needed.
  - The "Signals:" fired-list line (currently a single ' · '-joined run-on
    that can hit 150+ chars) must wrap cleanly at a fixed width instead of
    running off the edge — reuse _wrap() if it fits, or extend it.
  - Every section that prints a column table (short-term watchlist, whale
    activity) uses _render_table so column alignment is consistent and
    centrally fixable instead of duplicated per-section.
  - No section should produce a single line longer than 100 characters
    except the horizontal rule lines (_rule) themselves, which are
    intentionally full-width.

PART C.5 — NO BARE TICKERS: every row that names a market must also say what
that market IS. This is the fix for the specific complaint that motivated
this goal: a ticker like KXTAIWANLVL4-26JUL01 means nothing to a human
without its title. Fix each of the three locations found in PART A:
  1. BETTING QUEUE (_betting_queue in core/report.py): its SQL SELECT
     currently does not fetch `title`. Add `title` to the SELECT (the
     signals table already has this column — confirm via core/logger.py's
     schema before assuming; if title is not reliably populated for all
     rows, fall back to showing the ticker alone with a note rather than
     a blank/None string). Add a Title (or truncated Title) column to the
     queue's table output via _render_table, sized so the row still fits
     under the 100-char target — truncate title rather than blow the width
     budget, but never drop it to zero characters.
  2. SMART MONEY DRIFT table (in _smart_money_section): it has ticker but
     no title. The signals/snapshot data already carries kalshi_title in
     other parts of this same function (see the Kalshi Targets block) —
     thread kalshi_title through to the drift comparison's _curr/_prev
     keys or look it up by ticker from the grouped_signals data already
     available in the same call, and add a truncated title to each drift
     row.
  3. Per-Trader Cross-References table (also in _smart_money_section): it
     has kalshi_title available on each signal dict already (s["kalshi_title"]
     or similar — confirm exact field name) but never prints it. Add a
     truncated title column.
  For all three, truncate consistently with how other sections already
  truncate titles (existing _trunc usage elsewhere in this file is the
  precedent — match it rather than inventing a new convention) and prefer
  showing a shorter title over dropping it, removing other less-critical
  columns first if a row genuinely cannot fit everything under 100 chars.

PART C.6 — DEFINE AND ENFORCE A "WHAT A SELECTION NEEDS" CHECKLIST. Looking
at this report as an outside reader who did not write the code: any section
representing an actual model SELECTION (a recommendation to act, not just
surveillance/context) must show, at minimum: (1) BUY YES or BUY NO — the
side of the bet, stated plainly, not just implied by which column a number
sits in; (2) confidence level; (3) key stats — at least EV/contract and
edge; (4) a short analysis/reasoning fragment with its sources, when one
exists. Apply this distinction:
  - SELECTIONS (must meet the full checklist): NEW SIGNALS (_signal_block),
    REPEAT SIGNALS, TOP PICKS (_top_picks), and the BETTING QUEUE — all four
    represent an actual YES/NO call Leviathan is putting forward.
  - NOT SELECTIONS (checklist does not apply — these are surveillance/
    context, not recommendations, and must say so explicitly rather than
    imply a call exists): SHORT-TERM WATCHLIST and WHALE ACTIVITY are
    already correctly labeled "(no qualifying signal)" / context-only in
    their section headers — leave this framing intact, do not force
    BUY/confidence/EV onto rows that have no model estimate behind them, as
    that would mean fabricating a number that isn't there.
Audit each of the four SELECTION sections against the checklist and fix
what's missing:
  1. _signal_block: already shows BUY {direction}, confidence, EV, edge.
     Gap: when s.get("reasoning") or s.get("sources_checked") is empty, the
     block currently shows nothing at all for Analysis/Sources — a reader
     cannot tell "no analysis was generated" from "this signal has no
     supporting reasoning". Add an explicit fallback line, e.g.
     "  Analysis: (heuristic-only signal, no narrative reasoning generated)"
     when reasoning is empty but the signal exists, so absence is stated
     rather than silent.
  2. _top_picks: already shows BUY {direction}, confidence (via CONF_LABEL),
     edge, EV, Kelly. Gap: no analysis/sources at all, by design (it's a
     compact summary) — this is acceptable for a 1-2 line executive summary
     IF AND ONLY IF each top pick links back to its full _signal_block
     elsewhere in the same report (confirm this is true today; if top
     picks can include a signal that for some reason isn't also rendered
     in NEW/REPEAT SIGNALS, that's a separate bug — flag it, don't silently
     add full analysis text to the compact summary and break its purpose).
  3. REPEAT SIGNALS block (inline in compile_report, not a named function):
     shows BUY {direction}, confidence, and title/wrap — but no EV and no
     edge dollar value, only edge in percentage points, and no
     analysis/sources at all even when the original signal had them. Add
     EV/contract next to edge (reuse _ev_per_contract), and if the repeat
     signal's underlying dict still carries reasoning/sources_checked from
     when it was first logged, surface at least a one-line reasoning
     summary (not the full _wrap'd paragraph — this section is meant to
     stay compact) with a pointer like "(see original analysis above)" if
     it appeared earlier in the same report, or the short reasoning if not.
  4. BETTING QUEUE (_betting_queue): THIS IS THE WORST GAP. Its SQL SELECT
     already fetches `confidence` (line ~766) and unpacks it into a local
     `conf` variable (line ~782) — but `conf` is then NEVER added to the
     `candidates` dict and NEVER rendered in the table. `direction` is
     fetched and used internally for EV/Kelly math but is ALSO never shown
     in the table — a reader sees a ticker, price, and edge percentage but
     cannot tell from the table itself which side of the bet (YES or NO)
     is being recommended, or how confident the model was. Fix: add a
     "Side" (or "Dir") column showing BUY YES / BUY NO, and a "Conf" column
     showing the confidence label, to the table header and every row. Title
     (from PART C.5) must also be present. The queue is a compact table by
     design — full reasoning text doesn't belong here — but direction and
     confidence are not optional extras, they are the minimum a reader
     needs to act on a row, and they were already being fetched from the
     database and silently discarded before this fix.
After this part, every SELECTION section should let a reader who has never
seen the codebase answer, for any single row: what is being bet (title),
which side (YES/NO), how confident the model was, what the expected value
is, and — where available — why, with what it checked.

PART D — VERIFY VISUALLY, NOT JUST WITH ASSERTIONS. Generate a full sample
report by calling compile_report with synthetic but realistic signals,
whale_only, stats, run_meta, and config covering: an empty new-signals case,
a multi-flag signal like PART A's worst case, a repeat signal, a short-term
watchlist with several rows, at least one whale flag, at least one betting
queue candidate, and a smart money result with drift and per-trader data
populated. Print the FULL report to stdout in the transcript so a human
(not just a test assertion) can read it top to bottom. State the longest
line length found in this full run and confirm it's under the 100-char
target. Separately confirm, by eye, that the betting queue rows, smart
money drift rows, and per-trader cross-reference rows in this printed
output all show a recognizable title next to their ticker, AND that the
betting queue table now shows direction (BUY YES/NO) and confidence for
every row — not just that the code change exists, but that it actually
renders in the full report.

PART E — TESTS. Assert:
  - no line emitted by compile_report (excluding _rule() separator lines)
    exceeds 100 characters, across a signal dict engineered to trigger every
    optional label simultaneously
  - _close_and_urgency produces identical output to the OLD inline logic for
    a table of (close_time, now) pairs spanning each urgency threshold
    boundary (today/tomorrow, 3d, 7d, >7d, missing close_time) — write this
    as a regression test against hardcoded expected strings captured from
    current behavior BEFORE refactoring, so a silent behavior change fails
    the test
  - _render_table produces aligned columns for rows of varying content
    length, including a row with content longer than the column width
    (must truncate or wrap, not break alignment)
  - the betting queue's underlying query result (or the _betting_queue
    output itself) includes title text for a candidate, not just ticker —
    construct a test DB row with a known title and assert it appears in
    the rendered queue output
  - the smart money drift table's output contains a recognizable title
    fragment (not just a bare ticker) for a constructed drift case
  - the per-trader cross-reference table's output contains kalshi_title
    text for a constructed signal, not just kalshi_ticker
  - a row with a missing/empty title falls back gracefully (ticker alone,
    no crash, no literal "None" string printed)
  - the betting queue's rendered output shows BOTH a direction label
    (BUY YES or BUY NO) and a confidence label for a constructed candidate
    with known direction/confidence values — construct a test DB row with
    confidence="HIGH" and direction="YES" and assert both appear in the
    rendered queue table, not just in the underlying query result
  - a signal with empty reasoning still produces an explicit fallback
    "no narrative reasoning generated" (or equivalent) line rather than
    silently omitting the Analysis section entirely
  - a repeat signal's rendered block includes an EV/contract value (reuse
    of _ev_per_contract), not just edge in percentage points
  - all existing tests in tests/test_report.py still pass — if any existing
    test asserts on an exact old-format string that this goal intentionally
    changes, update ONLY that specific assertion to the new format and state
    in a comment why, rather than weakening the test
  - no test was deleted to make this pass
pytest exits 0 with output shown.

PART F — REPORT. Append to docs/PROGRESS.md: the PART A findings (max line
length before fix, list of duplicated logic locations, AND the list of
which sections were missing title context before this goal), the PART C.6
finding stated bluntly (the betting queue was fetching direction and
confidence from the database and discarding both before display — state
how long this had been true if determinable from git history, otherwise
just state the finding), the PART D max line length after fix and
confirmation all three previously-bare-ticker sections now show title AND
the betting queue now shows direction+confidence, and an explicit statement
that no underlying value, threshold, or data calculation was changed — the
only data-layer changes were adding an existing `title` column to the
betting queue's SELECT and surfacing `direction`/`confidence` values it
already had, both "surface what's already stored" fixes, not new
computation. Top 3 next steps should be about presentation polish that's
still open after this pass (e.g. "consider an HTML/markdown report variant
for email," "revisit whether 8 sections is the right count once goal 4b's
EV floor reduces queue noise," "confirm title is populated for all
historical rows once leviathan.db is available, in case older rows have
empty titles") rather than new features.

git add -A && git commit -m "Refactor report formatting: shared layout helpers, dedup close-time logic, bounded line widths, show market title and direction/confidence on every selection row"

Or stop after 20 turns.
