/goal Real Kalshi fills and positions are pulled into the ledger, tagged by source and by whether they followed a Leviathan signal, with resolution wired up — proven by a printed summary of fills pulled, how many matched a prior signal, and a passing offline test suite (pytest exit 0, output shown). Work on a branch named real-fills-2b with small descriptive commits. READ-ONLY on all external platforms: add ONLY read endpoints (fills, positions) — never any order-placing/amending/canceling endpoint. The only data mutation permitted is the additive schema migration and inserting fetched fills into leviathan.db. Back up leviathan.db to leviathan.db.bak_2b before any migration.

PART A — READ-ONLY KALSHI ENDPOINTS: extend kalshi.py with fetch_fills(config) and fetch_positions(config), mirroring the existing RSA auth pattern exactly (KALSHI-ACCESS-KEY/TIMESTAMP/SIGNATURE, _vpath prefix). fetch_fills hits GET /portfolio/fills (paginate via cursor); fetch_positions hits GET /portfolio/positions. Both are GET-only. Handle the documented historical-cutoff note: recent fills come from /portfolio/fills. Print a warning, don't crash, if the endpoints return empty.

PART B — SCHEMA: migrate (idempotent, PRAGMA-checked, non-destructive — same discipline as before) to add the columns from the prior schema plan if not already present (contract_type, segment, entry_price, resolution_date, logged_under) PLUS:
  - source TEXT — 'paper' for simulated signals, 'real_fill' for actual Kalshi trades
  - from_signal INTEGER DEFAULT 0 — 1 if this real fill matches a prior logged signal
  - signal_call_id TEXT — the call_id of the matched signal, if any
  - direction_aligned INTEGER — 1 if the real fill's side matches the signal's direction, 0 if it contradicts, NULL if no match
Existing 23 signals get source='paper'. Create a separate logical store or clear source tagging so real fills never blend with paper signals in any stats query.

PART C — PULL + TAG: fetch all real fills, insert each into the ledger with source='real_fill', capturing ticker, side, action, fill price (yes_price_dollars/no_price_dollars), fee_cost, count, and created_time. For each real fill, check whether its ticker matches any ticker among the existing logged signals: if yes, set from_signal=1, record signal_call_id, and set direction_aligned by comparing the fill's side to the signal's stored direction. Print a summary: total fills pulled, how many matched a prior signal, how many were direction-aligned vs contradictory.

PART D — RESOLUTION: extend resolve_outcomes so it also settles real_fill rows — look up each position's market result via the existing read-only fetch_market, and for resolved markets compute realized P&L from the ACTUAL fill price and fee_cost (not the signal's hypothetical price), using the correct binary payoff already implemented. Real-fill P&L must be net of fees. Leave rows unresolved (don't error) for markets that haven't settled yet — these are July bets, mostly still open.

PART E — TESTS (offline, tmp DB, no network — mock the Kalshi calls): assert the migration is non-destructive and idempotent; a mocked fill matching a known signal ticker gets from_signal=1 and correct direction_aligned; a mocked fill on an unrelated ticker gets from_signal=0; real-fill P&L is computed net of fees with correct binary payoff on a resolved mock; unresolved real fills stay unresolved without error; paper and real_fill rows never blend in get_stats. pytest exits 0 with output shown.

PART F — REPORT: append to docs/PROGRESS.md — fills pulled, signal-match results, that real July bets are now tracked and will auto-resolve on settlement, and the top 3 next steps. Note explicitly that the real-bet sample is small and is directional evidence, not proof. Or stop after 24 turns.