/goal A research-probe experiment exists that runs Claude+websearch over a stratified sample of markets — including ones the mechanical funnel rejects — logs each researched probability estimate against the market price as a forward-looking edge test, and is bounded in cost. Proven by: a completed first run printing the per-market divergence table and the total Claude/websearch cost or call count for that run, plus a passing offline test suite (pytest exit 0, output shown). Work on a branch named research-probe-2c with small descriptive commits. READ-ONLY on all platforms: market data and websearch only, never any order endpoint, no writes except inserting probe rows into leviathan.db (back up to leviathan.db.bak_2c first).

PART A — STRATIFIED SAMPLE: write analysis/research_probe.py that takes the saved market snapshot (or a fresh read-only fetch) and selects ~50 markets via STRATIFIED sampling that DELIBERATELY includes markets the mechanical filter_markets would reject — the point is to test for edge OUTSIDE the funnel. Stratify across: volume tiers (including thin/illiquid), categories, and time-horizon buckets (intraday through long). Print the strata breakdown so the sample composition is visible. Do NOT restrict to filter_markets survivors.

PART B — RESEARCH EACH MARKET: for each sampled market, call the Claude CLI with websearch (reuse scorer.py's mechanism) to produce a researched probability estimate, direction vs. the current market price, confidence, and a one-line rationale. CRITICAL COST CONTROL: this is the expensive step. Cap the run at the sample size, process sequentially, and TRACK the number of CLI/websearch invocations. Print the total call count (and any available cost/runtime) at the end so the per-run expense of the weekly cadence is known before committing to it. If the run would exceed a configurable max_probe_markets, stop at the cap.

PART C — LOG AS ITS OWN SEGMENT: insert each probe result into the ledger with source='research_probe' and segment='research_probe' — a FOURTH bucket that must never blend with paper, real_fill, or edge_thesis in any stats query. Record: ticker, market_price_at_probe, claude_estimate, divergence (claude_estimate - market_price), predicted_direction, confidence, resolution_date, timestamp. These are unresolved at log time — they get settled later by resolve_outcomes.

PART D — FORWARD SCORING (the actual experiment): extend resolve_outcomes (or a dedicated function) so that when probe markets resolve, it scores whether Claude's predicted direction was correct AND whether, on the markets with the LARGEST divergence from price, Claude beat the market. Add a stats function that — once probe rows resolve — reports: probe hit rate, hit rate among high-divergence calls specifically, and a plain statement of whether researched divergence predicted outcomes. Make explicit in code comments and output that run-one produces only hypotheses (divergences); the edge verdict requires resolved probe rows and is therefore pending until markets settle.

PART E — TESTS (offline, tmp DB, mock the CLI and market fetch, no network): assert the stratified sampler includes funnel-rejected markets; probe rows are tagged research_probe and never appear in paper/real_fill/edge_thesis stat queries; a mocked resolved probe row scores direction correctly; high-divergence subset stats compute correctly on mock data; the cap (max_probe_markets) is respected. pytest exits 0 with output shown.

PART F — REPORT: append to docs/PROGRESS.md — the sample composition, the run-one call count/cost, that divergences are logged and awaiting resolution, and an explicit note that this tests the core assumption (can AI research find mispricings the market hasn't priced) and that the answer is PENDING resolution, not yet known. Top 3 next steps. Or stop after 24 turns.