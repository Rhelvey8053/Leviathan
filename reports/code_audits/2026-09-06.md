# Weekly Code Audit — 2026-09-06

## Test suite
`py -m pytest tests/ -q`: **2376 passed, 1 skipped, 13 subtests passed, 0
failed** in 221.59s (3:41). Up from last audit's 2264 passed (0 failed);
no regressions.

## Scheduled task health
All 12 `Leviathan-*` tasks confirmed present (13 → 12 since last audit:
`Leviathan-SubscriberReport` was deliberately deregistered as part of the
subscriber-report feature removal — `backlog.json`'s
`subscriber-report-removed-2026-08` item explicitly lists "the
Leviathan-SubscriberReport scheduled task" among what was removed, so
today's "task not found" is expected, not a regression).

- **LogonType: all 12 confirmed `S4U`.** No regression to `Interactive`
  this week (the single most important check per this report's own
  instructions).
- **ExecutionTimeLimit**: all reasonable for their job shape —
  `DailyRun` PT2H, `CodeAudit` PT1H5M, `WeeklyAudit` PT30M,
  `SmartMoneyScan`/`ResolveFirst` PT10M, `GateNotifier`/`DailyDigest`/
  `PositionReconciliation`/`AutomationHealthCheck`/`Heartbeat`/
  `WakeCatchup` PT5M. `Litestream` is `PT0S` (unset) — confirmed fine,
  its Action is `litestream.exe replicate -config ...`, a genuine
  continuous daemon, not a one-shot job.
- **`Leviathan-GateNotifier` FAILED today (LastTaskResult 267014 =
  `SCHED_S_TASK_TERMINATED`) — hit its full 5-minute `ExecutionTimeLimit`
  and was force-killed.** Confirmed via
  `Microsoft-Windows-TaskScheduler/Operational`: launched 10:32:15 AM as
  part of today's mass wake-catchup (event 114, "could not launch as
  scheduled... started now" — same wave also caught `DailyRun`,
  `SmartMoneyScan`, `ResolveFirst`, `PositionReconciliation`,
  `DailyDigest`, `WeeklyAudit`, plus a dozen unrelated Windows/Office/
  Google/Zoom tasks, all within the same ~15 seconds). At 10:37:15 AM,
  event 329: "Task Scheduler terminated ... due to exceeding the time
  allocated for execution," action return code 2147943691. No double-fire
  this time (unlike 2026-08-26's mass-catchup) — each task in today's wave
  fired exactly once.
  - Read `scripts/gate_notifier.py` in full to trace what it could have
    been doing for 5 minutes. Ruled out: (1) an SMTP hang in
    `send_report()` — not reached, since no backlog gate is newly
    unlocked this run (see Backlog gates below), and the code path
    returns before ever calling `send_report()` when `newly_unlocked` is
    empty; (2) a SQLite lock-contention hang against `DailyRun`'s
    concurrent 22-minute write transaction (10:32:15–10:54:34, same
    window) — `core/logger.py`'s `_db()` uses bare `sqlite3.connect(DB_PATH)`
    with no explicit `timeout=`, so SQLite's own default 5-second busy
    timeout applies, and `get_stats()` wraps the query in `try/except`
    besides — too short and too guarded to explain a 5-minute hang.
    Genuine cause unconfirmed: `gate_notifier.py`'s Task Scheduler Action
    is a bare `python.exe` invocation with no `cmd.exe`/log-file
    redirection (same shape as `Leviathan-DailyDigest`, which completed
    fine in the same wave), so there is no captured stdout/stderr from
    the killed run to inspect.
  - **This is the second occurrence**, not a one-off: the same task
    hit the same `329` termination on 2026-08-30 8:50 AM. That date lines
    up with the already-logged (status: `ready`, not yet acted on)
    backlog item `windows-defender-cpu-contention-2026-08-30`, which
    documents system-wide slowdowns during heavy concurrent I/O
    (Defender real-time scanning + Docker Desktop background load) as
    the most plausible non-Leviathan-specific explanation for that day's
    stuck-looking runs. A live check today found Defender's `MsMpEng.exe`
    at only ~40 min cumulative CPU (not obviously pegged right now), so
    this doesn't confirm the theory live — but the timing match across
    two separate mass-wake-catchup episodes is suggestive. See Findings.
- **Battery-stop gap only partially fixed, still open on 10 of 12
  tasks.** `dailyrun-logontype-interactive` (done, 2026-08-27) fixed
  `StopIfGoingOnBatteries`/`DisallowStartIfOnBatteries` to `False` for
  `Leviathan-DailyRun` only, alongside the earlier, separate fix for
  `Leviathan-CodeAudit` — confirmed live, both now `False`. All 10 other
  tasks (`GateNotifier`, `SmartMoneyScan`, `ResolveFirst`, `DailyDigest`,
  `PositionReconciliation`, `AutomationHealthCheck`, `Heartbeat`,
  `WakeCatchup`, `WeeklyAudit`, `Litestream`) still have both settings
  `True` — confirmed live. This was flagged as "every setup_*.ps1 should
  add the flag" two audits ago (2026-08-26) and the backlog notes make
  clear the fix was deliberately scoped to just the two tasks that had
  actually been killed by it historically, not applied broadly. Not new,
  but still genuinely open — not fixed here per report-only constraints.
- No stuck/high-runtime-low-CPU processes found. Currently running:
  this very audit's own `Leviathan-CodeAudit` instance (started 11:00:00
  AM, expected/self-referential) and `litestream.exe` (PID 6772, running
  continuously since 8/23, actively appending to its log as of this
  check — healthy, not stuck).
- Two long-lived `notepad.exe` instances open since 8/23 and 8/24 with a
  Leviathan-path file argument, and an `mcp_server/server.py` python
  process + two Git Bash shells idle since 8/31 — none look like stuck
  scheduled-task instances (all pre-date today's runs by days), just
  long-running interactive sessions. Noting for awareness, not flagging
  as a problem.

## Data integrity
- `data/leviathan.db` `PRAGMA integrity_check`: **`ok`**.
- `data/powerbi_export/signals.csv`: 75 rows, `direction` values only
  `{YES, NO}` — split still intact, no `PASS` rows.
- `data/powerbi_export/scan_log.csv`: 743 rows, non-empty.
- Disk: `C:` free ≈ 36.1 GB (`Get-PSDrive C`) — well above the 10GB
  floor.
- **`logs/litestream.log` has grown to 47.2 MB (from 4.2MB at the last
  audit 2026-08-26, from 1.2MB the audit before that) — the no-rotation
  issue flagged twice already as "not urgent" has now compounded to a
  genuinely large file.** Growth rate is steady at roughly 4MB/day (INFO-
  level "replica sync" logged every ~2 seconds, unchanged). Confirmed
  still actively writing current timestamps, not stuck. Still not a disk-
  space emergency at 36GB free, but this is the third consecutive audit
  noting the same unbounded-growth pattern with no fix — worth actually
  addressing (log rotation, or dropping the per-2-second INFO line to a
  less verbose level) rather than re-flagging a fourth time.
- Other `logs/` files: sizes and last-write times all consistent with
  their tasks' schedules, nothing else frozen or oversized.
- `data/reconciliation/{2026-09-04,05,06}.json` (new, untracked):
  `misaligned: 0` and `unexpected: 0` on all three days, `unplaced` in
  the teens/twenties — expected shape for a paper-only bot, nothing
  alarming.

## Recent code changes since 2026-08-26
22 commits reviewed (`f72ce47..HEAD`, base commit identified as the one
that bulk-committed data through the last audit's date). Files touching
`core/` or `main.py`: `core/export_to_csv.py`, `core/fees.py`,
`core/kalshi.py`, `core/logger.py`, `core/report.py`, `core/scanner.py`,
`core/scorer.py`, `main.py`. No `scripts/setup_*.ps1` changes this
period.

- **`core/kalshi.py`'s `fetch_near_dated_markets()`** (fixing
  `kalshi-event-recency-window-misses-new-markets`): switched from a
  single global `target_count` cutoff (which let 1-2 abundant early days
  consume the whole per-run budget) to a fair per-day quota with
  surplus-backfill. Well-documented, live-verified numbers cited in the
  docstring (same page/market counts as before, now with previously-
  invisible NFL/NCAAF markets included). No partial wiring found.
- **`core/scanner.py`**: added a pre-check
  (`_is_two_way_matchup_phrasing`) ahead of the heuristic-rules table to
  catch a title-phrasing gap ("Will X win the Y vs Z game?") that was
  silently falling through to a generic 0.08-rate catch-all instead of
  the correct ~0.52 two-team-game rate. Both `estimate_base_rate()` and
  `get_heuristic_label()` were updated together — checked for the
  partial-wiring pattern this report always watches for, and both call
  sites are consistent.
- **`core/fees.py`**: new Polymarket fee-rate module (category-based
  taker fee schedule, cited as live-verified via web research
  2026-09-04) — additive, doesn't touch the existing Kalshi fee function.
- **`poly_net_price_gap`** (new field, backlog: `cross-venue-expansion`):
  traced its full path — computed in `sources/polymarket.py`, consumed in
  `main.py` (added at **both** of its two dict-literal call sites, not
  just one) and `core/scorer.py`'s prompt-building, persisted via
  **both** `log_signal()` and `log_pass()` in `core/logger.py` (column
  list, placeholder count, and bound-value tuple all updated together in
  both functions), and exported via `core/export_to_csv.py`'s whitelist.
  Covered by `tests/test_logger.py`, `tests/test_scorer.py`,
  `tests/test_polymarket.py`. No partial wiring found — this is exactly
  the bug class this report is instructed to hunt for, and it isn't
  present here.
- **`core/report.py`** (596 changed lines, mostly two commits: dropping
  whale/smart-money sections from the daily report + a weekly-digest
  reskin): read the substantive commit's diff and message in full. While
  fixing tests broken by the section removal, the same commit found and
  fixed 3 real `ticker=None`-vs-missing `.get()` crash sites in the
  weekly digest (same bug class as an earlier 2026-08-05 production
  crash) — and explicitly logged the *remaining* unaudited call sites as
  a new backlog item (`report-ticker-none-vs-missing-audit`) rather than
  silently leaving them or over-expanding scope. No swallowed exceptions
  found in the reviewed diff.
- Backlog doc-sync: `BACKLOG.md`'s section rows (`Ready` 9 + `Locked` 5 +
  `Blocked` 3 + `Done` 100 = 117) match `backlog/backlog.json`'s 117
  items, matching `tests/test_backlog.py`'s `== 117` assertion. The new
  `report-ticker-none-vs-missing-audit` item is properly recorded as
  `Ready` — no undocumented completed work found.

## Backlog gates
Live metrics via `backlog.checker.compute_metrics()` (never hand-rolled
SQL, per project convention):

| Item | Gate | Live value |
|---|---|---|
| calibration-curve | resolved_count >= 50 | 37 (up from 17 two audits ago) |
| slippage-tracking | fills_count >= 20 | 7 |
| per-wallet-track-record | resolved_count_per_wallet_max >= 10 | 0 (data gap: `smart_money_fills` table still missing) |
| skill-vs-luck-weighting | resolved_count_per_wallet_max >= 10 | 0 (same data gap) |
| empirical-base-rates-poly | sufficient_per_heuristic_label_resolved_data == 1 | never auto-computed (deliberate sentinel trigger — confirmed via `tests/test_backlog_checker.py`'s `test_sentinel_trigger_metric_never_auto_unlocks`, this is by design, not a bug) |

**No gates newly met this run.**

## Findings

1. **[Scheduled task, real, recurring] `Leviathan-GateNotifier` hung its
   full 5-minute execution window and was force-terminated during
   today's mass wake-catchup (10:32–10:37 AM), the second such
   termination after 2026-08-30.** Confirmed via Task Scheduler event
   log (`LastTaskResult`=267014/`SCHED_S_TASK_TERMINATED`, event 329
   "exceeding the time allocated for execution"). Traced through
   `gate_notifier.py`'s actual logic and ruled out both an SMTP hang
   (never reached — no gate newly unlocked this run) and a SQLite
   busy-timeout hang (capped at 5s, wrapped in try/except) as the cause;
   true root cause unconfirmed because this task's Action has no
   stdout/stderr log capture, unlike some siblings. Both occurrences
   line up with mass wake-catchup episodes and the already-logged (but
   still un-actioned, status `ready`) `windows-defender-cpu-contention-
   2026-08-30` backlog item. Worth either adding log redirection to this
   task's Action (so a third occurrence is diagnosable) or bumping
   `ExecutionTimeLimit` past 5 minutes if the real cause turns out to be
   benign system contention rather than an actual code hang.

2. **[Data, real, escalating] `logs/litestream.log` has grown to 47.2MB
   with zero rotation — the third consecutive audit flagging the same
   unbounded-growth issue, each time larger** (1.2MB → 4.2MB → 47.2MB
   across three audits). Not a disk-space emergency (36GB free), but the
   growth is real and monotonic; worth actually fixing (rotation config,
   or lowering the per-2-second "replica sync" line from INFO to a
   quieter level) rather than re-flagging a fourth time.

3. **[Scheduled task, real, still open] 10 of 12 Leviathan tasks still
   have `StopIfGoingOnBatteries`/`DisallowStartIfOnBatteries` = `True`.**
   Only `DailyRun` and `CodeAudit` — the two tasks actually killed by
   this in the past — were fixed; the fix was deliberately scoped
   narrowly per the backlog item's own notes, not applied to every
   `setup_*.ps1` as originally suggested. Not urgent (no confirmed
   battery-related kill on any of the other 10 to date), but worth a
   final decision either way (fix all, or explicitly close this as
   "narrow fix was the right call") rather than leaving it ambiguously
   open across three audits.

4. **[Info] Test suite clean** — 2376 passed, 1 skipped, 0 failed, up
   from 2264 passed last audit. No regressions.

5. **[Info] `poly_net_price_gap` (new cross-venue-fee field) is fully
   and correctly wired end-to-end** — computed, consumed at both call
   sites, persisted in both insert paths, exported, and tested. Called
   out specifically because this is exactly the partial-wiring bug class
   this report watches for, and it's clean this week.

6. **[Info] Backlog gates progressing but none newly met** —
   `resolved_count` more than doubled since two audits ago (17 → 37,
   still short of the 50 needed for `calibration-curve`); `fills_count`
   unchanged at 7 (needs 20); wallet-based gates still structurally
   blocked on the known missing-table gap. `empirical-base-rates-poly`'s
   apparent non-progress is by design (sentinel trigger), not a bug —
   confirmed against its own dedicated test.

7. **[Info] `Leviathan-SubscriberReport`'s "task not found" is expected**
   — deliberately deregistered as part of the subscriber-report feature
   removal (`backlog.json`: `subscriber-report-removed-2026-08`), not a
   silent regression.
