# Weekly Code Audit — 2026-08-24

## Test suite
`py -m pytest tests/ -q`: **2382 passed, 1 failed, 1 skipped, 13 subtests passed** in 477.88s.

Failure: `tests/test_backlog.py::test_parses_and_88_items` — `assert 89 == 88`.
`backlog/backlog.json` genuinely has 89 items (confirmed independently below). The
test's own docstring already has a paragraph explaining "89, not 88:
smart-money-fills-table-missing added 2026-08-25 ..." — so someone updated the
narrative but never updated the actual `assert` value (still `88`) or the
function name (still `test_parses_and_88_items`). Classic partial-wiring: one
sibling (docstring) updated, the other (assert/name) not.

## Scheduled task health
13 `Leviathan-*` tasks registered (the SessionStart hook's "7 healthy" only
sampled a subset — full list has 13, including Litestream, CodeAudit, WakeCatchup,
DailyDigest, AutomationHealthCheck not covered by that summary).

- **LogonType: all 13 are `S4U`.** No Interactive regression. This is the
  single most important check per the run instructions — clean this week.
- **ExecutionTimeLimit:** all reasonable (PT5M–PT30M) except:
  - `Leviathan-Litestream`: `PT0S` (unlimited) — verified legitimate: its
    action is `litestream.exe replicate -config tools\litestream.yml`, a
    genuine daemon process, matching the documented exception.
  - `Leviathan-CodeAudit`: **`PT30M` live** — see Findings #2, this is stale.
- **`Leviathan-DailyRun`** showed `State: Running` at the start of this audit
  (python.exe PID 3448, started 8:33:44 PM, 3m21s CPU accumulated over ~48min
  wall — not zero-CPU, so not a hang). Cross-checked via
  `Microsoft-Windows-TaskScheduler/Operational`: it completed normally with
  return code 0 at 9:23:24 PM, mid-audit. Not stuck.
- **`Leviathan-Litestream`**: `LastTaskResult` decodes to Win32 error 1067
  (`ERROR_PROCESS_ABORTED`) from 8/23 11:19:19 PM, but `RestartCount=3` /
  `RestartInterval=PT1M` kicked in — the current instance (PID 6772) has been
  running continuously since 8/23 11:19:21 PM, ~22h uptime as of this check.
  Self-healed by design; not a live problem.
- **`Leviathan-WakeCatchup`**: `LastRunTime` shows Windows' "never run"
  placeholder (11/30/1999) and result `SCHED_S_TASK_HAS_NOT_RUN`. Expected —
  it's a freshly-added event/logon/noon-triggered task (commits 1eb32cb,
  d72af72) that hasn't had a qualifying trigger yet. `NextRunTime` correctly
  shows tomorrow's noon fallback. Not broken, just unfired.
- All other 9 tasks: `LastTaskResult 0`, `LastRunTime`/`NextRunTime` consistent
  with their schedules. Healthy.

## Data integrity
- `data/leviathan.db` `PRAGMA integrity_check`: **`ok`**.
- `data/powerbi_export/signals.csv`: 51 rows, `direction` values are only
  `{NO, YES}` — no `PASS` rows, split still intact.
- `data/powerbi_export/scan_log.csv`: 468 rows, non-empty.
- Disk: `C:` free ≈ 45.1 GiB (48,379,387,904 bytes) — well above the 10GB
  floor.
- `logs/`: nothing frozen relative to schedule. `litestream.log` is 1.2MB and
  growing continuously with no visible rotation — not urgent at this size,
  worth watching over months. One zero-byte file,
  `weekly_code_audit_verify_20260824.log` (last write 9:11 PM today), matches
  commit a3383f9's own same-day verification attempt — consistent with that
  commit's message ("still produced no report") and this very run being the
  first to actually land one.

## Recent code changes since project start (no prior audit report existed)
Last 10 commits reviewed (`4dd1cf5`..`e90b208`). **None touch `core/` or
`main.py`** — all are automation/data/docs work (daily-digest restructure,
weekly_code_audit fixes, wake-catchup broadening, Dependabot config,
automation health check). Nothing to flag there.

One `scripts/setup_*.ps1` touched: `scripts/setup_weekly_code_audit_scheduler.ps1`
(commit `a3383f9`, made today 2026-08-24 21:11:11 -0500 — ~20 min before this
audit started). Explicitly specifies `-LogonType S4U` — good, matches the
required convention. See Findings #2 for the live/script mismatch this commit
introduced.

Backlog doc-sync: `BACKLOG.md`'s `## Done (73)` header matches its actual
73 table rows — no desync. `backlog/backlog.json` has 89 items, matching what
`test_backlog.py`'s docstring expects (see test failure above).
`fix-weekly-code-audit-timeout` (status `done`) and
`smart-money-fills-table-missing` (status `ready`) are both present and
correctly tracked — no undocumented completed work found.

## Backlog gates
5 locked items, none newly met:

| Item | Gate | Live value |
|---|---|---|
| edge-decay-analysis | resolved_count >= 30 | 17 |
| calibration-curve | resolved_count >= 50 | 17 |
| slippage-tracking | fills_count >= 20 | 7 |
| per-wallet-track-record | resolved_count_per_wallet_max >= 10 | 0 (table missing) |
| skill-vs-luck-weighting | resolved_count_per_wallet_max >= 10 | 0 (table missing) |

The `resolved_count_per_wallet_max` pin at 0 is because `smart_money_fills`
doesn't exist in the live DB (confirmed: `sqlite_master` lookup returns no
row) — this is the already-tracked `smart-money-fills-table-missing` backlog
item, not a new finding. No threshold-lowering proposed, per project policy.

## Findings

1. **[Test suite] `test_backlog.py::test_parses_and_88_items` fails —
   assert/function-name not updated to match its own docstring.** The
   docstring already documents "89, not 88" (smart-money-fills-table-missing
   added 2026-08-25); `backlog/backlog.json` genuinely has 89 items
   (independently verified). Only the `assert 89 == 88` line and the function
   name (`..._88_items`) still say 88. One-line-plus-rename fix once someone
   picks it up — not doing it here per report-only constraints.

2. **[Scheduled task] `Leviathan-CodeAudit`'s live `ExecutionTimeLimit` is
   still 30 minutes; commit `a3383f9` (today, 21:11) raised the intended
   value to 65 minutes in `scripts/setup_weekly_code_audit_scheduler.ps1`,
   but the setup script was never re-run to apply it to the actual registered
   task.** Confirmed via `Microsoft-Windows-TaskScheduler/Operational`: on
   2026-08-23 ~12:13 PM this exact task was killed by Task Scheduler
   ("terminated ... due to exceeding the time allocated for execution") under
   the old 30-min limit — the precise failure `a3383f9` says it fixed. As
   currently registered, a run that takes 30–65 minutes will hit the same
   wall again at the next scheduled fire (2026-08-30 11:00 AM), silently
   undoing the fix. Needs `scripts/setup_weekly_code_audit_scheduler.ps1`
   re-run against the live scheduler (not done here — Task Scheduler
   modification is out of scope for this report-only run).

3. **[Info] Backlog gates unchanged this week** — resolved_count (17),
   fills_count (7) both well below their thresholds; wallet-based gates
   structurally blocked pending the already-tracked missing-table fix.
   Nothing actionable.

4. **[Info] Litestream had one crash+auto-restart on 8/23 11:19 PM**
   (Win32 1067 / `ERROR_PROCESS_ABORTED`), self-healed via its own
   `RestartCount`/`RestartInterval`, running cleanly ~22h+ since. No action
   needed, noted for the record in case the stale `LastTaskResult` is
   noticed elsewhere.

5. **[Info, process note] This session's PowerShell/Bash tool access denied
   any command using `$_`, script blocks, `foreach`/`ForEach-Object`, `@{}`
   hashtables, or `::` static calls — single flat pipelines and simple `py -c`
   scripts (no comprehensions) worked fine.** All checks above were still
   completed by working around this, but it took materially more tool calls
   than the prompt's checklist implies. Worth knowing if a future unattended
   run of this same prompt appears to stall or under-report — it's a tool
   permission characteristic of the run environment, not a task or script bug.
