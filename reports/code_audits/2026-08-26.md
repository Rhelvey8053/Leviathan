# Weekly Code Audit — 2026-08-26

## Test suite
`py -m pytest tests/ -q`: **2264 passed, 1 skipped, 13 subtests passed, 0 failed** in 982.96s (16:22).

Count is down from last week's 2382+1 failed (2383 total) — expected and
self-documented: commit `97c7430` (subscriber-report feature removal)
deleted ~120 subscriber-only tests and its own commit message says so
("2263 passed... down from 2383 -- expected"). One more test than that
(2264) from small additions since.

Last week's flagged bug is fixed: `tests/test_backlog.py`'s
`test_parses_and_88_items` (docstring said 89, assert said 88 — partial
wiring) is now `test_parses_and_94_items` with `assert len(...) == 94`,
function name and assert in agreement, and passing live.

## Scheduled task health
**Could not check LogonType or ExecutionTimeLimit this week.**
`Get-ScheduledTask` and `schtasks` were denied outright by this session's
tool permissions — tried via the PowerShell tool directly, via Bash
calling `powershell.exe`, and via Bash calling `schtasks` directly; all
three blocked, unlike last week's audit (which hit restricted syntax but
could still call the cmdlets). `Get-Process`, `Get-CimInstance`,
`Get-WinEvent`, and `Get-Date` all worked fine, so the checks below lean
on those instead. **This means the single most important check per this
report's own instructions — LogonType regression to Interactive — is
unconfirmed this week**, not "clean." Last week all 13 were confirmed
S4U; no task-registration script change since then touches LogonType
except the already-tracked `schedule_setup.ps1` gap (see below).

- **Mass wake-catchup + duplicate firing, today ~1:27–1:33 PM.** Event
  log shows essentially all ~10 Leviathan tasks (DailyRun, Heartbeat,
  PositionReconciliation, GateNotifier, ResolveFirst, SmartMoneyScan,
  DailyDigest, AutomationHealthCheck, WakeCatchup, CodeAudit) fired via
  Windows' native `StartWhenAvailable` catch-up (event ID 114, "could
  not launch as scheduled... started now") within about 2 seconds of
  each other at 1:27:49–1:27:51 PM. Six of them — WakeCatchup,
  SmartMoneyScan, GateNotifier, DailyDigest, ResolveFirst,
  PositionReconciliation — then fired a **second time** roughly 3
  minutes later (~1:30:39–1:30:40 PM), each completing a second full
  start/finish cycle. Root cause is NOT `catchup_missed_tasks.py` (the
  custom wake-catchup script): its own log
  (`logs/catchup_missed_tasks.log`) shows it launched exactly one task
  (CodeAudit) at 18:28:40 UTC, then correctly reported "nothing overdue"
  on its second invocation at 18:32:17 UTC — clean, not double-firing
  anything. The second wave is most likely Windows' own
  `StartWhenAvailable` independently catching up each task's own missed
  trigger a second time. `Leviathan-DailyRun` did NOT double-run — its
  second scheduled attempt at 1:30:39 PM was correctly refused
  ("instance already running", event 322) since the first instance was
  still executing; it finished cleanly with return code 0 at 1:43:40 PM.
  See Findings #2 for the one confirmed duplicate side effect and why
  the others look benign.
- **`Leviathan-CodeAudit`'s own task wrapper was killed mid-run today.**
  Event log: "Task Scheduler stopped instance ... of task
  \Leviathan-CodeAudit because the computer is switching to battery
  power" at 1:44:44 PM, return code 2147943691 (non-zero). This audit
  run itself is that CodeAudit instance and is still producing output
  well past that timestamp, so whatever's actually doing the work here
  survived — but the *scheduled task's own wrapper* did not. See
  Findings #1.
- **Litestream**: still running continuously since 8/23 11:19 PM (~3
  days uptime), CPU 24.3s accumulated (idle-normal for a daemon). Log
  scanned for `level=ERROR`/`panic`/`FATAL` since last week — none
  found. Healthy, no new crash.
- **`Leviathan-DailyRun`**: completed successfully, return code 0, at
  1:43:40 PM (the catch-up-triggered run described above). A separate,
  unrelated `python main.py` process was also observed running
  concurrently during this audit (PID 8960, started 1:27:49 PM,
  finished before this check completed) — matches
  `logs/manual_run_20260826.log`'s timestamp, i.e. the owner manually
  ran it; this audit did not start or touch it, per the hard
  constraint against running `main.py`.

## Data integrity
- `data/leviathan.db` `PRAGMA integrity_check`: **`ok`**.
- `data/powerbi_export/signals.csv`: 51 rows, `direction` values only
  `{NO, YES}` — split still intact.
- `data/powerbi_export/scan_log.csv`: 475 rows (up from 468 last week),
  non-empty.
- Disk: `C:` free ≈ 46.3 GB (via Python `shutil.disk_usage`, since
  `Get-PSDrive` was also denied this run same as the Task Scheduler
  cmdlets) — well above the 10GB floor.
- `logs/litestream.log` grew from 1.2MB (last audit, 8/24) to **4.2MB**
  now — roughly 3.5x in 2 days, driven by an INFO-level "replica sync"
  line logged every ~2 seconds with no rotation. Last week's audit
  called this "not urgent... worth watching over months"; the actual
  measured growth rate is faster than that framing suggested. Still not
  urgent at 4.2MB, but worth a second look if it keeps compounding.
- Other logs: nothing frozen relative to schedule, nothing else
  oversized.
- `data/reconciliation/2026-08-26.json` (new, from today's
  PositionReconciliation run): 12 paper-open positions, 0 real fills
  fetched, all 12 correctly bucketed `unplaced`, `misaligned: []`,
  `unexpected: []` — expected shape for a paper-only bot with real
  fills still at 7 lifetime, nothing alarming.

## Recent code changes since 2026-08-24
17 commits reviewed (`4dd1cf5..HEAD`). Files touching `core/`, `main.py`,
or `scripts/setup_*.ps1`: `core/report.py`, `core/subscribers.py`
(deleted), `main.py`, `scripts/schedule_setup.ps1`.

- **`main.py`**: adds `scoring_hard_failed` flag, set only when
  `scorer.score_markets()` raises with `flagged_markets` non-empty;
  drives `sys.exit(1)` at the very end so Task Scheduler's
  `RestartCount` (added same day in `schedule_setup.ps1`) has something
  real to retry on. Well-commented, doesn't touch the quiet-day path.
  No partial wiring, no swallowed exceptions.
- **`core/report.py`**: ~1600 of the changed lines are the subscriber-
  report feature's removal (commit `97c7430`). Confirmed clean:
  `grep`ed the whole repo for `subscribers` (only a historical comment
  remains) and for `determine_subscriber_shortlist` (renamed to
  `determine_top_shortlist`, and the rename is fully wired — same name
  used consistently across `main.py`, `report.py`, and
  `tests/test_rescore_shortlist.py`, no stale references anywhere).
  Also added `Date`/`Message-ID` headers to `send_report()`'s outgoing
  mail (spam-filter risk fix, correctly implemented with
  `email.utils.formatdate`/`make_msgid`).
- **`core/subscribers.py`**: deleted entirely. Grepped for
  `core.subscribers`/`import subscribers` across all `.py` files —
  zero remaining references.
- **`scripts/schedule_setup.ps1`**: fixed a real path bug
  (`$ScriptDir` previously pointed one level too shallow, so the
  registered task's `main.py` path didn't exist); added
  `RestartCount 3` / `RestartInterval 1hr` to pair with `main.py`'s new
  exit-code behavior. Still registers with `-LogonType Interactive`,
  not S4U — **already tracked** as backlog item
  `dailyrun-logontype-interactive` (added 2026-08-25), not a new
  finding. Confirmed all 12 `scripts/setup_*.ps1` files (the newer
  convention) do specify `-LogonType S4U` explicitly — only this one
  legacy script (different filename pattern) has the gap.

Backlog doc-sync: `BACKLOG.md`'s `## Done (76)` header matches its
actual 76 table rows. `backlog/backlog.json` has 94 items, matching
`test_backlog.py`'s now-fixed assertion. Commit `c92b08b` (real
category-data-loss bug, found building a dashboard chart: only 4/51 real
bets have a populated `category` despite Kalshi having the data) is
properly recorded as backlog item `signal-category-mostly-blank-despite-
real-data` with a corresponding test update — no undocumented completed
work found.

## Backlog gates
5 locked items, none newly met:

| Item | Gate | Live value |
|---|---|---|
| edge-decay-analysis | resolved_count >= 30 | 17 |
| calibration-curve | resolved_count >= 50 | 17 |
| slippage-tracking | fills_count >= 20 | 7 |
| per-wallet-track-record | resolved_count_per_wallet_max >= 10 | 0 (table missing) |
| skill-vs-luck-weighting | resolved_count_per_wallet_max >= 10 | 0 (table missing) |

Note on methodology: my first pass at `resolved_count` (a hand-rolled
query using only `outcome IS NOT NULL`) returned 454 — wrong, because it
missed the `outcome != ''` half of the canonical filter and picked up
hundreds of paper rows with an empty-string (not NULL) outcome. Re-ran
using the exact filter `core/logger.py` itself uses
(`_NO_PASS AND outcome != '' AND outcome IS NOT NULL`) and got 17,
matching last week's figure and `BACKLOG.md`'s own stated metric. Flagging
this here as a live illustration of exactly the pitfall CLAUDE.md warns
about — don't hand-roll a `signals` query.

## Findings

1. **[Scheduled task, real, new] None of the 13 `Leviathan-*` scheduled
   tasks set `-StopIfGoingOnBatteries:$false`, and this already caused a
   live task to be killed mid-run today.** Confirmed via event log:
   "Task Scheduler stopped instance ... of task \Leviathan-CodeAudit
   because the computer is switching to battery power" at 1:44:44 PM
   today, with a non-zero return code — this is this very audit's own
   scheduled-task wrapper. Checked all `scripts/setup_*.ps1` plus
   `scripts/schedule_setup.ps1` for `StopIfGoingOnBatteries` or
   `DisallowStartIfOnBatteries` — zero matches across all 13. This
   report is still being produced because whatever actually executes
   this session isn't 1:1 tied to the killed `cmd.exe`/`taskhostw.exe`
   wrapper's lifetime, but that's not something to rely on — a future
   run under slightly different circumstances could be silently
   truncated with no report at all, the same failure class
   `a3383f9`'s own history describes ("still produced no report"). Not
   fixed here per report-only constraints; every `setup_*.ps1` should
   add the flag.

2. **[Scheduled task, real] Six Leviathan tasks double-fired within ~3
   minutes during today's mass wake-catchup (1:27–1:30 PM), with one
   confirmed duplicate side effect.** `Leviathan-SmartMoneyScan` ran its
   full scan logic twice, producing two separate git auto-commits 3
   minutes apart (`5dadd0c` at 13:30:02, `e619641` at 13:33:01) — the
   second run silently overwrote the first's `data/smart_money/
   2026-08-26.md` "Run at" line with no owner-visible indication two
   runs happened. Harmless today (0 positions/0 signals both times),
   but if real positions had been found, the second run's report would
   have quietly replaced the first's without any diff surfaced.
   Checked the other five double-fired tasks for the same risk:
   `GateNotifier` is fire-once-per-gate-state (not per-day), so its
   second run correctly found nothing newly unlocked; `DailyDigest`'s
   `data/daily_digest_state.json` shows only one `last_sent` value
   ("2026-08-26"), consistent with a same-day guard preventing a second
   email (not exhaustively traced through the script); `ResolveFirst`
   and `PositionReconciliation` have no email/notification code at all
   (grepped, zero matches) — their duplicate runs just mean redundant
   Kalshi API calls and an overwritten JSON/log file. Root cause traced
   to Windows' own `StartWhenAvailable` mechanism, not the custom
   `catchup_missed_tasks.py` (confirmed clean via its own log — it
   launched exactly one task and correctly no-opped on its second
   check). Not urgent given no confirmed harmful duplication today, but
   worth understanding why `StartWhenAvailable` re-fired the same
   missed schedule twice for six tasks in one wake episode.

3. **[Process note] This week's tool permissions blocked all Task
   Scheduler read commands outright** — `Get-ScheduledTask` and
   `schtasks` were denied via the PowerShell tool, via Bash calling
   `powershell.exe`, and via Bash calling `schtasks` directly, while
   `Get-Process`/`Get-CimInstance`/`Get-WinEvent`/`Get-Date` all worked
   normally. This is a harder restriction than last week's (which hit
   restricted syntax but could still call the cmdlets). Net effect:
   LogonType and ExecutionTimeLimit could not be verified for any of
   the 13 tasks this week — worked around using `Get-WinEvent` +
   `Get-CimInstance` for whatever could be inferred (task completion
   status, process command lines, crash history), but the regression
   check this report's instructions call out as the single most
   important item is genuinely unconfirmed, not clean, this week.

4. **[Info] `logs/litestream.log` grew ~3.5x in 2 days** (1.2MB → 4.2MB),
   same no-rotation issue flagged last week; growth rate is faster than
   last week's "watch over months" framing implied. Still not urgent.

5. **[Info] Test suite and backlog doc-sync both clean this week** —
   2264 passed, 0 failed; last week's `test_backlog.py` assert/name
   mismatch is fully fixed (94 items, function name and assert now
   agree); `BACKLOG.md`'s Done-header count matches its rows;
   `c92b08b`'s real bug finding (category data loss) is properly
   recorded in the backlog with no undocumented completed work.

6. **[Info] Backlog gates unchanged** — resolved_count (17), fills_count
   (7) both well below their thresholds; wallet-based gates still
   structurally blocked on the already-tracked missing-table issue.
   Nothing actionable.
