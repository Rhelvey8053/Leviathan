# Weekly Audit — 2026-08-01

## Test suite
2065 passed, 1 skipped, 13 subtests passed (205.5s). No failures. (2064
passed pre-fix; +1 for the new `test_sports_award_recalibration` test added
this run — see "Drafted fix" below.)

## Calibration (heuristic_backtest.py)
- Pre-fix: overall heuristic-table Brier 0.0837 (naive baseline 0.1786),
  directional accuracy 89.0% (n=2096), coverage 2154/12600 (17.1%) — matches
  the prior `reports/audits/2026-08-01.md` run exactly, no drift.
- Post-fix (after the sports-award recalibration below): Brier improved to
  **0.0824**, directional accuracy holds at 89.0%, coverage unchanged
  (2154/12600 — a rate re-tune, not a label change).
- Note: an earlier pass at today's audit slot produced a
  `reports/audits/2026-08-01.md` that was missing the "Drafted fix this run"
  section required by the audit template (skipped the calibration-fix
  investigation entirely). This run redoes the audit and completes that
  step; this file replaces it.

## Backlog gates
Queried `data/leviathan.db` read-only. No gates newly met (unchanged from
the prior audit):
- `resolved_count` (signals where `direction != 'PASS' AND result != ''`) = **15**.
  Below every threshold that depends on it: brier-tracking/confluence-detection
  (>=25), edge-decay-analysis (>=30), calibration-curve (>=50).
- `resolved_count_per_category_max` (by `heuristic_label`, non-PASS resolved) = **2**
  (`cabinet departure`). Below the >=15 threshold for per-heuristic-scorecard
  and heuristic-sunsetting.
- `fills_count` (signals with `source='real_fill'`) = **7**. Below the >=20
  threshold for slippage-tracking.
- `resolved_count_per_wallet_max` (per-wallet-track-record, skill-vs-luck-
  weighting) not queryable — `signals` has no per-wallet identity column,
  only aggregate `smart_money_count`/`smart_money_dir`. Doesn't change the
  outcome since resolved_count is already far below every other gate.
- BACKLOG.md's Ready section is empty (0 items) — nothing to skim there.

## Git / repo state
- Branch `report-readability-4c`, up to date with origin. `git log -10`
  shows routine data/calibration commits, nothing stale.
- Untracked, uncommitted infrastructure: `scripts/weekly_audit.py`,
  `scripts/weekly_audit_prompt.md`, `scripts/setup_weekly_audit_scheduler.ps1`,
  `scripts/setup_position_reconciliation_scheduler.ps1`, `logs/`, and
  `data/reconciliation/2026-08-02.json`. None of this is reflected in
  BACKLOG.md's Done section yet (position-reconciliation-job is still listed
  at Priority 4, not Done, despite a live scheduler + output file existing).
- `reports/heuristic_backtest.md` shows as modified — expected, it's the
  backtest script's own output file, regenerated twice this run (once
  pre-fix, once post-fix).

## Drafted fix this run
**Label: `sports award`** (n=107, pre-fix gap +0.163, |gap|×n≈17.4 — the
largest-impact qualifying candidate this run; `down-ballot election` also
qualified, see Findings).

- **Root cause**: many-way field, same shape as win-catchall/show-renewal/
  first-named-storm. All 107 real settled-market matches for the keyword
  group (`mvp`, `cy young`, `rookie of the year`, `heisman`, `hall of fame`,
  `all-star`, `golden glove`, `best player`) traced via `event_ticker`
  grouping to exactly 4 tickers: `KXMLBASGMVP-26` (n=36), `KXWNBACCUPMVP-26`
  (n=27), `KXWCAWARD-26GGLOVE` (n=23), `KXNBASUMMERMVP-2026` (n=21) — 100%
  of matches, no mixed/ladder sub-pattern. Each ticker is one award with
  ~21-36 named-nominee markets and exactly 1 YES per ticker (the actual
  winner) — 4/107 YES overall (3.7%), vs. the old flat rate of 20%.
- **Fix**: re-tuned the flat rate 0.20 → 0.04 (measured 3.7%, rounded to
  match the first-named-storm precedent for a similarly-sized many-way
  field). Did not touch the keyword list or label.
- **Verification before drafting**: throwaway script compared
  `get_heuristic_label`/`estimate_base_rate` across all 12,600 settled-market
  titles under the current table vs. a simulated table with only this rate
  changed. Exactly the 107 `sports award`-labeled titles changed (rate only,
  label unchanged); zero other titles affected.
- **Changes**: `core/scanner.py` (`_HEURISTIC_RULES` rate + dated comment),
  `tests/test_scanner.py` (new `test_sports_award_recalibration`, plus
  updated the 3 existing sports-award assertions in the parametrized rate
  table from 0.20 → 0.04).
- **Test results**: `py -m pytest tests/ -q` → 2065 passed, 1 skipped, 13
  subtests, 100% green.
- **Before/after calibration**: label's own gap +0.163 → +0.003 (predicted
  4.0% vs actual 3.7%), label's own Brier 0.0624 → 0.0360. Overall
  heuristic-table Brier 0.0837 → 0.0824, directional accuracy unchanged at
  89.0%.
- **Left uncommitted** as a working-tree diff (`core/scanner.py`,
  `tests/test_scanner.py`) for human review.

`down-ballot election` (n=43, gap +0.287, |gap|×n≈12.3) was investigated but
**not drafted** — disqualified under rule 4's first bullet (mixed
sub-patterns, no dominant root cause). 43 matches span 11 distinct
`event_ticker`s: one dominant ticker, `KXGOVCA-26` (23/43, 53%), alongside
10 other tickers each contributing just 2 matches (`KXHOUSERACE-CA*-26`,
`KXGAPRIMARY1R-26MAY19`, `HOUSECA40-26`). 53% is well under the 90%
single-cause threshold, and the shapes look structurally different (one
large many-candidate primary field vs. many small 2-candidate general-
election races) — exactly the "genuinely mixed, no dominant explanation"
case the prompt says to report instead of fix. Flagged for a future run
with a human's judgment on whether/how to split it.

## Findings
- `data/reconciliation/2026-08-02.json` (position-reconciliation's latest
  run) shows `positions_fetched: 0` against `paper_open: 13`, so all 13 open
  paper signals land in `unplaced` — 0% match rate. Not root-caused here
  (out of scope, never runs the underlying fetch); worth a human check on
  whether that's expected ("no real capital deployed yet") or a broken
  position fetch — if the latter, the reconciliation scheduler is producing
  a file every run without ever reconciling anything.
- `down-ballot election` (see "Drafted fix" above) is a real, sizeable
  calibration gap (+0.287, n=43) that a future audit or a human should
  revisit — likely needs splitting into a many-way-field sub-rule
  (`KXGOVCA-26`-style primaries) and a 2-way-race sub-rule, which is exactly
  the kind of precedence-ordering judgment call this unattended run is not
  allowed to make.
- BACKLOG.md's header metrics (`resolved=12, fills=7`) and this audit's own
  DB query (`resolved_count=15`, `fills_count=7`) still disagree by 3 on the
  resolved-signal count — same discrepancy noted in the prior audit attempt,
  still unresolved. Likely a different filter definition rather than a bug,
  but flagged again since it's what a human skims for gate-proximity.
- Otherwise a quiet week: tests green, calibration improved by the one
  drafted fix, no gates crossed.
