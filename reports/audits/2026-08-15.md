# Weekly Audit — 2026-08-15

## Test suite
**Could not run.** This session's Bash tool has neither `git` nor `py`
reachable on PATH — confirmed both directly and via an independent,
freshly-spawned diagnostic subagent (separate process/context), which hit
the identical failure:
- `git --version` → `/usr/bin/bash: line 1: git: command not found` (exit 127)
- `py -m pytest tests/ -q`, `py -c "..."`, `py analysis/heuristic_backtest.py`
  → all exit 127, `py: command not found`
- Other invocation variants (`python`, `python3`, `git.exe`, full paths under
  `/c/Windows/`, `powershell -Command ...`) were rejected outright by this
  session's command allowlist before even reaching the PATH problem.
- `dangerouslyDisableSandbox` made no difference — this is a real PATH/
  environment gap in the shell this session was given, not a permission
  question I can negotiate around.
- No local `.venv`, no bundled `python.exe`/`Scripts/*.exe` anywhere in the
  repo tree (checked via Glob) that could be invoked by full path instead.

No test run was performed. Pass/fail counts from this run: none available.

## Calibration (heuristic_backtest.py)
**Could not run**, same root cause as above. No Brier score or directional
accuracy for this run; no comparison to `reports/audits/2026-08-01.md`
possible beyond noting its last recorded numbers for reference: overall
heuristic-table Brier 0.0824, directional accuracy 89.0% (n=2096), post the
sports-award-recalibration fix from that run.

## Backlog gates
**Could not independently query `data/leviathan.db`** — the read-only
`py -c "SELECT COUNT(*)..."` one-liner this step requires hit the same
execution blocker. The only figure available is BACKLOG.md's own
self-reported header, unverified this run: `Last updated: 2026-08-04 |
Metrics: resolved=27, fills=7`. Per the 2026-08-01 audit, this header has
previously disagreed with a live DB query by 3 (header said 12, live query
said 15) — so I am explicitly **not** treating 27/7 as ground truth, only
reporting it as what the file currently claims.

Of the 7 Locked gates, only 3 are even checkable against the header number
(the other 4 use different metrics the header doesn't contain at all):
- Checkable against `resolved=27, fills=7` (unverified): `calibration-curve`
  (`resolved_count >= 50` — 27 < 50, below), `edge-decay-analysis`
  (`>= 30` — 27 < 30, below), `slippage-tracking` (`fills_count >= 20` —
  7 < 20, below). All three appear to remain unmet under the header number,
  but **this is not a verified finding** since the header itself is
  unverified this run.
- Not checkable at all this run — no header figure exists for these
  metrics: `per-heuristic-scorecard` / `heuristic-sunsetting`
  (`resolved_count_per_category_max >= 15`) and `per-wallet-track-record` /
  `skill-vs-luck-weighting` (`resolved_count_per_wallet_max >= 10`). These
  4 gates have literally no data point available this run, verified or
  not — not "appears unmet," just unknown.

Bottom line: no confidently-known newly-unlocked gates either way. A human
should re-run the DB query once the environment is fixed. BACKLOG.md's
Ready section is currently empty (0 items) — nothing to skim there.

## Git / repo state
Live `git log`/`git status` could not be run this session (see above). Using
the git snapshot the harness provided at session start instead (not
independently re-verified, may be a few hours stale):
- Branch `report-readability-4c`.
- Recent commits: `0498e6e` smart money scan 2026-08-16, `4f2adda` migrate to
  kalshi-sdk + whale-flag guarantee, `e402268` multi-sample-scoring,
  `06ec42f`/`5e11c36` confluence-detection/brier-tracking to Done — all
  consistent with routine, already-landed work, nothing that looks like a
  stale WIP branch.
- Working tree at session start showed uncommitted modifications to
  `BACKLOG.md`, `backlog/backlog.json`, `core/report.py`,
  `data/Leviathan Dashboard.pbix`, `data/powerbi_export/signals.csv`,
  `data/smart_money/2026-08-03.md`, `data/smart_money/latest_signals.json`,
  `data/whale_history/streak.json`, `reports/flag_modes.md`,
  `reports/threshold_sweep.md`, `tests/test_backlog.py`,
  `tests/test_report.py`, plus untracked
  `data/reconciliation/2026-08-03.json`/`2026-08-04.json`/`2026-08-16.json`,
  `data/smart_money/2026-08-04.md`/`2026-08-05.md`, `logs/`, and
  `scored_markets_output.json`. This audit made **zero** edits to any of
  these — none of it originated in this session. Most looks like normal
  scheduled-task output (smart-money scans, reconciliation snapshots,
  dashboard refresh), but `core/report.py`, `tests/test_backlog.py`, and
  `tests/test_report.py` being modified in the working tree is worth a
  human's eye — that's application/test code sitting uncommitted, not just
  a data artifact, and this run has no way to tell whether it's finished
  work awaiting commit or an interrupted edit.

## Drafted fix this run
**No fix drafted.** Step 3 of the "drafting a calibration fix" procedure
requires a throwaway verification script (running
`get_heuristic_label`/`estimate_base_rate` under current vs. simulated
tables) and step 5 requires a clean `py -m pytest` pass before leaving any
diff in place — both require the same blocked `py` execution path. With no
working test runner, drafting any edit to `core/scanner.py` this run would
violate the hard constraint "if your test suite run fails... REVERT" (I
would have no way to confirm it *didn't* fail), so per rule 4's last bullet
("any part of steps 1-3 raises something you're not fully certain of →
report instead of drafting") this run correctly reports instead of drafting.
`core/scanner.py` and `tests/` are untouched — no diff to revert, nothing
uncommitted from this run.

## Findings
- **Primary finding, blocks this entire audit**: this session's Bash tool
  has no working `git` or `py`/`python` on PATH, confirmed via direct
  invocation and an independently-spawned diagnostic subagent hitting the
  identical failure in a separate process. Every other scheduled Leviathan
  task that shells out to `py` (DailyRun, SmartMoneyScan, ResolveFirst,
  GateNotifier, SubscriberReport, PositionReconciliation) reported "ok" at
  session start per the startup hook, which suggests those run under a
  different execution context than this interactive/agent session got —
  worth a human checking whether the WeeklyAudit task specifically is
  launched with a different (broken) PATH than the others, since this looks
  like an environment-provisioning gap for this task rather than a
  repo-code problem.
- `core/report.py`, `tests/test_backlog.py`, `tests/test_report.py` are
  modified in the working tree with no commit — see "Git / repo state"
  above. Worth a human check on whether this is finished, ready-to-commit
  work or something interrupted mid-edit.
- BACKLOG.md's header (`resolved=27, fills=7`) could not be cross-checked
  against a live DB query this run, continuing the unresolved discrepancy
  the 2026-08-01 audit already flagged between the header and the real
  count — still worth a human resolving the definition mismatch once the
  environment issue is fixed, so the header can be trusted at a glance
  again.
- No calibration regression check, no gate-unlock check, and no calibration
  fix could be attempted this week — this audit should be considered
  incomplete, not "a quiet week." Recommend re-running once `git`/`py` are
  confirmed reachable from this task's execution context.
